---
name: implement-tasks
description: Implements the tasks in docs/tasks
---

## Process

- Collect all the open tasks in `docs/tasks`
- Order them based on the `Blocked by` dependency
- Implement each of the tasks one by one
- Only continue to the next one after verifying the task is completed
- If you fail to finish a task, abort and inform the user