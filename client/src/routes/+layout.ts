// This tells SvelteKit to prerender all pages at build time
// and generate static HTML files
export const prerender = true;
export const ssr = false; // Disable server-side rendering for a pure SPA
